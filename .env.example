APP_ENV=development
# Use DATABASE_URL in production (for example, the Supabase transaction pooler).
DATABASE_URL=
DATABASE_PATH=./finovate.db
TRANSCRIPT_INGEST_API_KEY=replace-with-a-random-secret
CORS_ORIGINS=http://localhost:3000
